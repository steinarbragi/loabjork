import project from './project';
import publication from './publication';
import bio from './bio';
import gallery from './gallery';
import shop from './shop';

export const schemaTypes = [project, publication, bio, gallery, shop];
